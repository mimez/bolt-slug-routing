Bolt Slug Routing Extension
===========================

This bolt extensions modifies the URLs to contents. Instead of /{contentTypeSlug}/{slug}/ (e.g. /page/testpage) the URL
is now /{slug} (e.g. /testpage).